import SwiftUI

struct V4Brand: AppBrand {
  let cornerRadius = CornerRadiusSpec(extraLarge: 24, large: 16, medium: 8, small: 4)
  let spacing: SpacingSpec = SpacingSpec(extraLarge: 24, large: 16, medium: 8, small: 4)
}
