import Bonzai
import SwiftUI

struct ActivatePreviousWorkspaceCommandView: View {
  var body: some View {
    Text("Switch between the current and previous Workspace back and forth.")
      .style(.derived)
  }
}
