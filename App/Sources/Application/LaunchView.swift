import SwiftUI

struct LaunchView: View {
    var body: some View {
        Text("Loading ...")
    }
}

struct LaunchView_Previews: PreviewProvider {
    static var previews: some View {
        LaunchView()
    }
}
